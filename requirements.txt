# EchoMind-PM 基础依赖（开箱即用）
# 安装：pip install -r requirements.txt
# 说明：本清单不包含 redis / chromadb，未安装时系统自动降级为内存/关键词模式。
# 需要完整版（Redis 工作记忆 + ChromaDB 向量检索/情景记忆）时，另装 requirements-full.txt

# ── AI / LLM ──────────────────────────────────────────────────────────────────
openai>=3.0.0              # OpenAI 兼容接口（豆包 Ark / 官方 / 第三方中转站），需 >=3.0 支持异步 Responses API

# ── Web 框架 ──────────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn==0.32.1              # 核心版（standard extras 在 Python 3.14 上无预编译包）
pydantic>=2.12,<3            # 需 >=2.12 以支持 Python 3.14（2.10.x 无 cp314 wheel）
websockets>=12.0             # WebSocket 支持（uvicorn 标准 extras 的替代）
python-multipart==0.0.12    # FastAPI 文件上传支持

# ── 监控 ──────────────────────────────────────────────────────────────────────
prometheus-client==0.21.1

# ── HTTP 客户端 ───────────────────────────────────────────────────────────────
httpx==0.28.1              # monitor/performance_monitor.py webhook 告警

# ── 工具 ──────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── 飞书集成 ──────────────────────────────────────────────────────────────────
lark-oapi>=1.4.0            # 飞书开放平台 SDK（机器人消息 + 多维表格操作）
